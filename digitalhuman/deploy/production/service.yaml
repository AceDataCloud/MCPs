apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-digitalhuman
  name: mcp-digitalhuman
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-digitalhuman
