apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-recaptcha
  name: mcp-recaptcha
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-recaptcha
