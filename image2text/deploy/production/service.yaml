apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-image2text
  name: mcp-image2text
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-image2text
