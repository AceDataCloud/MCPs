apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-seedream-pro
  name: mcp-seedream-pro
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-seedream-pro
