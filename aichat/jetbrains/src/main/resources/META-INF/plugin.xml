<idea-plugin>
    <id>com.acedatacloud.mcp.aichat</id>
    <name>AiChat MCP</name>
    <vendor email="dev@acedata.cloud" url="https://acedata.cloud">Ace Data Cloud</vendor>

    <description><![CDATA[
<p><b>Unified OpenAI-compatible LLM gateway</b> via <a href="https://platform.acedata.cloud">Ace Data Cloud</a> — chat with GPT, Claude, Gemini, Grok, DeepSeek, Kimi and more through a single MCP endpoint.</p>

<p>Once configured, AI Assistant can chat with 50+ LLM models — GPT, Claude, Gemini, Grok, DeepSeek and more — through a single endpoint.</p>

<h3>Features</h3>
<ul>
    <li><b>4 AI Tools</b> — Create conversation, List models, Usage guide</li>
    <li><b>Easy Setup</b> — One-click copy MCP configuration for AI Assistant</li>
    <li><b>Dual Mode</b> — STDIO (local) and HTTP (remote) server support</li>
    <li><b>Token Management</b> — Store API token securely in IDE settings</li>
</ul>

<h3>Available Tools</h3>
<ul>
    <li><b>Create conversation</b> — Chat with any supported LLM via a single tool</li>
    <li><b>List models</b> — Discover the full catalog of available models</li>
    <li><b>Usage guide</b> — Built-in documentation for every supported model</li>
</ul>

<h3>Quick Start</h3>
<ol>
    <li>Install this plugin</li>
    <li>Open <b>Settings → Tools → AiChat MCP</b></li>
    <li>Enter your <a href="https://platform.acedata.cloud">Ace Data Cloud</a> API token</li>
    <li>Click <b>Copy Config</b> and paste into <b>Settings → Tools → AI Assistant → MCP</b></li>
</ol>

<p>Requires <a href="https://github.com/astral-sh/uv">uv</a> (Python package runner) for STDIO mode,
or use HTTP mode for zero-install remote access.</p>
]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationService
            serviceImplementation="com.acedatacloud.mcp.aichat.McpSettings"/>

        <applicationConfigurable
            id="com.acedatacloud.mcp.aichat.settings"
            displayName="AiChat MCP"
            parentId="tools"
            instance="com.acedatacloud.mcp.aichat.McpSettingsConfigurable"/>

        <postStartupActivity
            implementation="com.acedatacloud.mcp.aichat.McpStartupActivity"/>

        <notificationGroup id="AiChat MCP" displayType="BALLOON"/>
    </extensions>

    <actions>
        <group id="com.acedatacloud.mcp.aichat.Actions" text="AiChat MCP" popup="true">
            <action id="com.acedatacloud.mcp.aichat.CopyStdioConfig"
                class="com.acedatacloud.mcp.aichat.CopyStdioConfigAction"
                text="Copy STDIO Config"
                description="Copy STDIO MCP configuration for AI Assistant"/>
            <action id="com.acedatacloud.mcp.aichat.CopyHttpConfig"
                class="com.acedatacloud.mcp.aichat.CopyHttpConfigAction"
                text="Copy HTTP Config"
                description="Copy HTTP MCP configuration for AI Assistant"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </group>
    </actions>

    <change-notes><![CDATA[
<h3>0.1.0</h3>
<ul>
    <li>Initial release</li>
    <li>MCP server integration with JetBrains AI Assistant</li>
    <li>STDIO and HTTP transport modes</li>
    <li>Settings UI for API token management</li>
    <li>One-click MCP config copy</li>
</ul>
]]></change-notes>
</idea-plugin>
