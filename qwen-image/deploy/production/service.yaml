apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-qwen-image
  name: mcp-qwen-image
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-qwen-image
