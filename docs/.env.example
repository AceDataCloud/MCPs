# AceData Docs MCP is PUBLIC — no API token required.
# Override only if you need to point at a non-default base URL.
ACEDATACLOUD_API_BASE_URL=https://api.acedata.cloud
MCP_SERVER_NAME=AceData Docs
MCP_TRANSPORT=stdio
LOG_LEVEL=INFO
